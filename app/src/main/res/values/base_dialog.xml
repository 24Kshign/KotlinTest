<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="dialog_from_bottom_anim">
        <item name="android:windowEnterAnimation">@anim/bl_dialog_from_bottom_anim_in</item>
        <item name="android:windowExitAnimation">@anim/bl_dialog_from_bottom_anim_out</item>
    </style>

    <style name="dialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <!-- 边框 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 是否浮现在activity之上 -->
        <item name="android:windowIsTranslucent">true</item>
        <!-- 无标题 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!-- 背景透明 -->
        <item name="android:backgroundDimEnabled">true</item>
        <!-- 模糊 -->
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="md_dialog" parent="Theme.AppCompat.Dialog">
        <item name="android:windowFrame">@null</item>  <!-- 是否有边框 -->
        <item name="android:windowIsFloating">true</item>  <!-- 是否浮在Activity上 -->
        <item name="android:windowNoTitle">true</item>    <!-- 是否浮显示标题 -->
        <item name="android:windowIsTranslucent">true</item>    <!-- 是否半透明 -->
        <item name="android:backgroundDimEnabled">true</item>  <!-- 是否模糊显示 -->
    </style>

    <style name="dialog_scale_anim">
        <item name="android:windowEnterAnimation">@anim/bl_dialog_scale_anim_in</item>
        <item name="android:windowExitAnimation">@anim/bl_dialog_scale_anim_out</item>
    </style>
</resources>